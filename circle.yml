machine:
  node:
    version: 4.2.1
deployment:
  release:
    tag: /[0-9]+(\.[0-9]+)*/
    commands:
      - echo $CIRCLE_BRANCH
      - echo $POSTOP_URL
      - curl -H "Content-Type:\ application/json" -d '{"version":"'$CIRCLE_BRANCH'"}' -X POST $POSTOP_URL
