machine:
  node:
    version: 4.2.1
deployment:
  release:
    tag: /[0-9]+(\.[0-9]+)*/
    commands:
      - echo 'version:${CIRCLE_BRANCH}'
      - echo $POSTOP_URL
      - curl -X POST -d 'version:${CIRCLE_BRANCH}' $POSTOP_URL
