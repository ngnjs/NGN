var Base = require('../util/Server');

/**
 * @class NGN.http.Socket
 * Placeholder for a socket server.
 * @author Corey
 */
var Class = Base.extend({
	
	constructor: function(config){
		
		Class.super.constructor.call(this,config);
		
		//TODO: Fill in
	}
	
});

module.exports = Class;